// Formas de declarar função

//* Function Declaration (posso chamar ela antes de declarar)

function soma(x, y) {
  return x + y
}

//* function Expression

const sub = function (x, y) {
  return x - y
}

//* named function expression
const mult = function mult(x, y) {
  return x * y
}
